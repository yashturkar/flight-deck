# Open Questions

- Which files or paths should Builder avoid by default?
- Should PR `#12` be closed or recreated from a non-`main` head branch so the admin UI work can be reviewed as a normal branch line?
